import { StyleSheet, Text, View } from 'react-native'
import React from 'react'
import ScrollSlider from './AnimScreens/ScrollSlider'

export default function App() {
  return (
    <View style={{flex: 1}}>
      <ScrollSlider />
    </View>
  )
}

const styles = StyleSheet.create({})